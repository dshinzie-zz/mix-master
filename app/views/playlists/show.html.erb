<h1><%= @playlist.name %></h1>

<ul>
  <% @playlist.songs.each do |song| %>
    <li><%= link_to song.title, song %></li>
  <% end %>
</ul><br/>

<%= link_to "Edit", edit_playlist_path(@playlist) %><br/><br/>

<%= link_to "Back", playlists_path %>
